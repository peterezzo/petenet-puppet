---
classes:
    - apt

apt::update:frequency: daily

base::os_added_packages:
    - puppet
    - augeas-tools
    - screen
    - traceroute
    - git

base::os_removed_packages:
    - resolvconf
    - popularity-contest
