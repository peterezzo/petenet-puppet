---
:backends:
    - yaml
:yaml:
    :datadir: /etc/puppet/hieradata
:hierarchy:
    - "node/%{::fqdn}"
    - "role/%{::role}"
    - "hostgroup/%{::hostgroup}"
    - "virtual/%{::virtual}"
    - "os/%{::operatingsystem}"
    - common
