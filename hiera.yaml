---
:backends:
    - yaml
:yaml:
    :datadir: /etc/puppet/hieradata
:hierarchy:
    - "node/%{::fqdn}"
    - "virtual/%{::virtual}"
    - "os/%{::operatingsystem}"
    - "hostgroup/%{::hostgroup}"
    - "role/%{::role}"
    - common
