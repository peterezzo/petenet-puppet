---
classes:
  - apt

apt::update:frequency: daily

base::packages:
  vim: {}
  resolvconf:
    ensure: 'purged'
  popularity-contest:
    ensure: 'purged'

base::files:
  '/etc/update-motd.d/10-help-text':
    ensure: 'absent'

base::sudo_env_keep: true