---
:backends:
    - yaml
:yaml:
    :datadir: /etc/puppet/hieradata
:hierarchy:
    - "node/%{::fqdn}"
    - "role/%{::role}"
    - "hostgroup/%{::hostgroup}"
    - "os/%{::operatingsystem}"
    - "virtual/%{::virtual}"
    - common
